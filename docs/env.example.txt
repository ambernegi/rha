# Environment variables (example)
#
# Note: We cannot add `.env.example` in this repo because `.env*` files are blocked/ignored.
# Copy these keys into Vercel Project Settings → Environment Variables, and into `.env.local` for local dev.

# Supabase (client)
NEXT_PUBLIC_SUPABASE_URL="https://YOUR_PROJECT.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="YOUR_SUPABASE_ANON_KEY"

# Supabase (server-only)
SUPABASE_SERVICE_ROLE_KEY="YOUR_SUPABASE_SERVICE_ROLE_KEY"

# Single host/admin identity (used by seed/manual setup)
HOST_EMAIL="owner@example.com"


